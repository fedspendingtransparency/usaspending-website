import React from 'react';
import PropTypes from 'prop-types';

import CheckboxTree from 'components/sharedComponents/CheckboxTree';

const propTypes = {};

const TASCheckboxTree = (props) => {
    return (
        // <CheckboxTree />
        "YO"
    );
};

export default TASCheckboxTree;
